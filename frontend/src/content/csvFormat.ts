// Shared between the always-visible upload hint and the post-error callout
// so the two descriptions of the expected CSV shape can't drift apart.
export const CSV_FORMAT_HINT =
  'Expected columns: a card name column (name or card name) and a quantity column (count, quantity, or qty).';
